import React from "react";

export default function RoutineCertification() {
  return (
    <>
      <div className="Certification">
        {/* data?.projectparticipants.map((elm) => (
          <>
            {loginState?.id === data?.projectparticipants ?
              <MemberCard
                elm={elm}
                key={elm.profile.id}
                data={data}
                loginState={loginState}
                setApplyFlag={setApplyFlag}
              />
              <Certification
                date
          </>

        )) */}
      </div>
    </>
  );
}
